#include <iostream>
#include <string>
#include <vector>
#include <numeric>
#include <algorithm>
#include <map>
#include <queue>

using namespace std;

int main() {
    for(int i=0; i < 10 ; i++ ){
        cout << i << endl;
    }
    for(int i=0; i < 10 ; ++i ){
        cout << i << endl;
    }
}
